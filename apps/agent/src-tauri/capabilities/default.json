{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the desktop agent window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
